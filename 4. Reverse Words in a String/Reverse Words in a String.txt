class Solution {
    public String reverseWords(String s) {
        String word="";
        String[] arr;
        for(int i=0; i<s.length(); i++)
        {
            char ch = s.charAt(i);
            if (ch!=' ')
            {
                word=word+ch;
            }
                
    }
}